/*
   In order to use the function 'printf', '#include <stdio.h>'
   must be stated at the beginning of the file as the printf
   function is only available from that file.
   stdio.h is A header file used to work on A console screen.
*/

#include <stdio.h>

int main()
{
  printf("Text printed to terminal with A newline following.\n");
  return(0);
}
