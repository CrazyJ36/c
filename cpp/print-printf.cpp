#include "iostream"
// 'using namespace std' statement not required in this case.
// probably because i'm using 'printf()', 'cout <<'
// probably requires it.
// TODO: confirm.
int main(void)
{
  // 'printf()', alternative to 'cout()'.
  // seems 'endl' won't work with 'prinf()'
  // nor without 'using namespace std'
  printf("text printed using printf()\n");
  return 0;
}
